# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code local tooling (skills, launch config, local settings)
.claude/

# Node (once a real app exists)
node_modules/
dist/
build/
.env
.env.*

# Office temp files
~$*.docx
~$*.xlsx

# Firebase local deploy cache
.firebase/

# Private / non-source files (keep out of git history)
Quantica_Founder_Agreement.docx
_anim.json
