# Pinned to the version already proven on this machine, Python 3.14 arm64. manim, pycairo
# and manimpango all ship cp314 wheels at these versions, which is unusually lucky for a
# Python this new, so do not bump casually without re-testing that the wheels still exist.
manim==0.20.1
