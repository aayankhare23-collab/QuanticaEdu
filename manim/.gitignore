# The venv, every render, and the finished cuts. None of this belongs in git, and
# firebase.json ignores manim/** so none of it reaches production hosting either.
.venv/
media/
out/*
!out/.gitkeep
__pycache__/
*.pyc
