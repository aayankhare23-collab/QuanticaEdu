[CLI]
# Pin the output inside manim/ so no render can write to the repo root. manim's default
# media_dir is ./media relative to the working directory, and a stray glyph cache at
# repo-root media/ was being served from production until 2026-08-09.
media_dir = ./media
frame_rate = 30
